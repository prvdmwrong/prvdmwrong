local configure = require("@lune-lib/configure")

return configure.package({
	name = "compat-quill",
	description = "https://github.com/featherfall-org/quill compatible replacement using Prvd 'M Wrong",

	pesdeTargets = {
		roblox = true,
	},
})
