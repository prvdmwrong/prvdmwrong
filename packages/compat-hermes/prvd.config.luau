local configure = require("@lune-lib/configure")

return configure.package({
	name = "compat-hermes",
	description = "https://github.com/froststarinteractive/hermes compatible replacement using Prvd 'M Wrong",

	pesdeTargets = {
		roblox = true,
	},
})
