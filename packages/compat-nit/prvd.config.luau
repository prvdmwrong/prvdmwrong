local configure = require("@lune-lib/configure")

return configure.package({
	name = "compat-nit",
	description = "https://github.com/ratplier/nit compatible replacement using Prvd 'M Wrong",

	pesdeTargets = {
		roblox = true,
	},
})
